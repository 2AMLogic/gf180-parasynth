__pycache__/
*.pyc
.DS_Store
.venv/
audio/
*.wav
refaudio/cache/
*.vvp
rtl-sketch/build/
pnr/orfs/work/
pnr/klt/*/.klt/
fpga/build/
pnr/orfs/collect.out
pnr/orfs/collect.sh
pnr/orfs/joined.sh

# top-level verifier output directories (make controls writes build/<variant>/)
/build/

# render/analyse/accept manifest artifacts (tools/manifest.py, issue #68):
# plain-file CI artifacts with their own retention rules, not committed here.
/runs/
/jobs/
# no trailing slash: `.venv/` matches a directory but NOT a symlink to one,
# which is how a venv symlink got staged by `git add -A` on another branch
.venv
build/

# the frozen reference profile: hashes and provenance are committed, audio is not
refprofile/cache/

# Pinned CI simulator source and release binary cache.
.tools/verilator-5.052/
# >>> loom-managed (do not edit) >>>
# Loom runtime state (don't commit these)
.loom-in-use
.loom-checkpoint
.loom-managed
.no-changes-needed
.snapshots/
.loom/.daemon.pid
.loom/.daemon.log
.loom/daemon.sock
.loom/daemon-loop.pid
.loom/daemon-metrics.json
.loom/loom-source-path
.loom/spawn-loop-state.json
.loom/spawn-loop.pid
.loom/stop-spawn-loop
.loom/issue-failures.json
.loom/interventions/
.loom/worktrees/
.loom/worktrees-local/
.claude/worktrees/
.loom/state.json
.loom/mcp-command.json
.loom/activity.db
.loom/claims/
.loom/locks/
.loom/signals/
.loom/status/
.loom/retry-state/
.loom/exit-codes/
.loom/sweep-checkpoint/
.loom/sweep-run/
.loom/state/ci-telemetry/
.loom/state/fleet-captain/
.loom/concierge/
.loom/stats/
.loom/diagnostics/
.loom/guide-docs-state.json
.loom/metrics_state.json
.loom/manifest.json
.loom/stuck-config.json
.loom/metrics/
.loom/usage-cache.json
.loom/claude-config/
.loom/native-tools/
.loom/tokens/
.loom/accounts.env
.loom/api-keys/
.loom/account-health.json
.loom/account-health.lock
.loom/CANARY
.loom/*.log
.loom/*.sock
.loom/*.tmp
.loom/*.bak
.loom/.install.lock
.loom/logs/
.loom/.resync-in-progress
.loom/gh-config/
.loom/gh-config-by-owner/
.loom-local/
# <<< loom-managed <<<
